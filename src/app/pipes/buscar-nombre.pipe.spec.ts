import { BuscarNombrePipe } from './buscar-nombre.pipe';

describe('BuscarNombrePipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarNombrePipe();
    expect(pipe).toBeTruthy();
  });
});
