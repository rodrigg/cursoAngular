import { PriorizarTodosPipe } from './priorizar-todos.pipe';

describe('PriorizarTodosPipe', () => {
  it('create an instance', () => {
    const pipe = new PriorizarTodosPipe();
    expect(pipe).toBeTruthy();
  });
});
