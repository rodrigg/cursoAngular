import { TerminadosTodosPipe } from './terminados-todos.pipe';

describe('TerminadosTodosPipe', () => {
  it('create an instance', () => {
    const pipe = new TerminadosTodosPipe();
    expect(pipe).toBeTruthy();
  });
});
